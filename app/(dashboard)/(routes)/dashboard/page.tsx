import { Button } from "@/components/ui/button";

const Dashboard = () => {
  return (
    <Button variant={"destructive"} size={"lg"}>
      Click me
    </Button>
  );
};

export default Dashboard;
